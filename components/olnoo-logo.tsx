export function OlnooLogo({ className }: { className?: string }) {
  return (
    <span
      className={`font-mono text-[15px] font-medium tracking-[0.18em] text-foreground ${className ?? ''}`}
    >
      ØLNOO
    </span>
  )
}
